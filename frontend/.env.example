# URL base del API para el browser (islands). Vacío → /api/v1 (relativa; proxy vite en dev).
PUBLIC_API_BASE_URL=
# URL base del API para SSR (ficha). Solo dev; en prod la provee systemd (INTERNAL_API_BASE_URL).
INTERNAL_API_BASE_URL=http://127.0.0.1:8000/api/v1
